apiVersion: v1
kind: Service
metadata:
  name: {{ include "sjrk-story-telling.fullname" . }}
  labels:
    {{- include "sjrk-story-telling.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sjrk-story-telling.selectorLabels" . | nindent 4 }}
